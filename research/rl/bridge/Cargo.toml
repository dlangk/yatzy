[package]
name = "yatzy-rl-bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
yatzy = { path = "../../backend" }
rand = { version = "0.9", features = ["small_rng"] }
rayon = "1.10"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
