//! Rosetta Stone: policy distillation into human-readable skill ladder.
//!
//! Converts the exact DP oracle into a semantic decision list — an IF-ELIF-ELSE
//! rule set optimized for EV-regret minimization using human-interpretable features.

pub mod dsl;
pub mod policy;
