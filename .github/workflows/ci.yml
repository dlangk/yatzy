name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  rust:
    name: Rust (solver)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: solver
      - name: Format check
        run: cargo fmt --check
        working-directory: solver
      - name: Clippy
        run: cargo clippy --release -- -D warnings
        working-directory: solver
      - name: Test
        run: cargo test
        working-directory: solver

  frontend:
    name: Frontend (TypeScript)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
        working-directory: frontend
      - name: Lint
        run: npx eslint src/
        working-directory: frontend
      - name: Type check
        run: npx tsc --noEmit
        working-directory: frontend
      - name: Test
        run: npx vitest run
        working-directory: frontend

  treatise:
    name: Treatise (build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: treatise/package-lock.json
      - name: Install
        run: npm ci
        working-directory: treatise
      - name: Build
        run: node build.mjs
        working-directory: treatise
